- first pass
- `rg TODO`
- there is a bug in `make fmt` after first checkout (see `g why` to repro)
- all things in my triton-service.md notes 
- should we specify usage and preferred modules for some things?
    var assert = require('assert-plus');
    var uuidv4 = require('uuid/v4');
    var vasync = require('vasync');
    var VError = require('verror');
- spec error handling for modules?
